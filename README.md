Hello Git-it World

changing this for fun

commands
 
"touch" to find or make a file;
"open" to open a file;
"cd" change directory;

git commands
"status" to see whats happening
"commit -m [message]"
